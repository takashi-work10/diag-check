"use client";

import { Box, Typography, List, ListItem, ListItemText } from '@mui/material';

const blogs = [
    
]

export default function ArticlePage() {

    return (
        <Box sx={{ height: "100vh" }}>
            <Typography sx={{ mt: 8 }}>記事</Typography>
            <List>

            </List>
        </Box>
    );
}